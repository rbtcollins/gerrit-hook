============
Installation
============

At the command line::

    $ pip install gerrit-client-hooks

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gerrit-client-hooks
    $ pip install gerrit-client-hooks
