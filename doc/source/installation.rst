============
Installation
============

At the command line::

    $ pip install gerrit-hook

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gerrit-hook
    $ pip install gerrit-hook